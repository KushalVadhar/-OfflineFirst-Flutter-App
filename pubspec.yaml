name: offline_first
description: Offline-first article app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.10"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cloud_firestore: ^4.13.6
  firebase_core: ^2.24.2
  connectivity_plus: ^5.0.2
  uuid: ^4.2.2
  logger: ^2.0.2+1
  equatable: ^2.0.5
  intl: ^0.19.0

dev_dependencies:
  hive_generator: ^2.0.1
  build_runner: ^2.4.7
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.1

flutter:
  uses-material-design: true
